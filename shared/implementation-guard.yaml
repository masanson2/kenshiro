requires:
  current_phase: IMPLEMENTATION
  gates:
    stack: APPROVED
    impact: APPROVED
    tasks: APPROVED
  artifacts:
    - stack.yaml
    - feature.yaml
    - impact.yaml
    - tasks.yaml
deny_action: source_modification
on_denial:
  workflow_status: BLOCKED
  activity_event: Implementation Blocked
